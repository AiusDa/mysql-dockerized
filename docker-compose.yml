version: '3.7'
services:
  mysql:
    container_name: mysql
    image: mysql:5.6
    restart: always
    ports:
      - ${EXPOSE}:3306
    environment:
      - MYSQL_ROOT_PASSWORD
    volumes:
      - ./dumps:/dumps
      - ./lib:/var/lib/mysql
